import type {HoleScore,Round,RoundTotals} from "./types";
export function holeDecisionWins(h:HoleScore){return Number(h.plan)+Number(h.decision)+Number(h.commit)+Number(h.emotion)}
export function calculateTotals(round:Round):RoundTotals{const gross=round.holes.reduce((s,h)=>s+h.strokes,0),putts=round.holes.reduce((s,h)=>s+h.putts,0),penalties=round.holes.reduce((s,h)=>s+h.penalties,0),heroAttempts=round.holes.filter(h=>h.heroAttempt).length,decisionWins=round.holes.reduce((s,h)=>s+holeDecisionWins(h),0),decisionPossible=72,decisionPercent=Math.round(decisionWins/decisionPossible*100),doublesOrWorseEstimate=round.holes.filter(h=>h.strokes>0&&h.strokes-h.par>=2).length;return{gross,putts,penalties,heroAttempts,decisionWins,decisionPossible,decisionPercent,doublesOrWorseEstimate,grade:grade({decisionWins,penalties,heroAttempts,doublesOrWorseEstimate})}}
function grade(i:Pick<RoundTotals,"decisionWins"|"penalties"|"heroAttempts"|"doublesOrWorseEstimate">):RoundTotals["grade"]{if(i.decisionWins>=66&&i.penalties===0&&i.heroAttempts===0&&i.doublesOrWorseEstimate===0)return"A";if(i.decisionWins>=60&&i.penalties<=1&&i.heroAttempts<=1&&i.doublesOrWorseEstimate<=1)return"B";if(i.decisionWins>=52&&i.penalties<=2&&i.heroAttempts<=2)return"C";return"D"}
