"use client"; import {Award,Save} from "lucide-react"; import {useRoundStore} from "../store/round-store"; import {Button} from "@/ui/button";
export function DebriefPanel({onArchive}:{onArchive:()=>void}){const {round,totals,updateReflection}=useRoundStore();return <section className="space-y-3"><div className="rounded-app border border-parchment-200 bg-parchment-50 p-5 shadow-soft"><div className="flex items-center gap-3"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-ink text-white"><Award className="h-7 w-7"/></div><div><h2 className="text-2xl font-black">Round Debrief</h2><p className="text-sm text-stone-600">Turn the score into a plan.</p></div></div><div className="mt-4 grid grid-cols-2 gap-2"><M label="Decision" value={`${totals.decisionWins}/${totals.decisionPossible}`}/><M label="Grade" value={totals.grade}/><M label="Doubles+" value={totals.doublesOrWorseEstimate}/><M label="Hero" value={totals.heroAttempts}/></div></div><Field label="What decision saved the most shots?" value={round.reflection.bestDecision} onChange={v=>updateReflection('bestDecision',v)}/><Field label="What decision cost the most shots?" value={round.reflection.costliestDecision} onChange={v=>updateReflection('costliestDecision',v)}/><Field label="One commitment for next round" value={round.reflection.nextRoundCommitment} onChange={v=>updateReflection('nextRoundCommitment',v)}/><Button variant="primary" className="w-full justify-center" onClick={onArchive}><Save className="h-4 w-4"/>Archive Round</Button></section>}
function M({label,value}:{label:string;value:string|number}){return <div className="rounded-2xl border border-parchment-200 bg-white/80 p-4 text-center shadow-card"><div className="text-3xl font-black">{value}</div><div className="mt-1 text-[10px] font-black uppercase tracking-widest text-stone-500">{label}</div></div>}
function Field({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="block rounded-app border border-parchment-200 bg-parchment-50 p-4 shadow-card"><div className="mb-2 text-[10px] font-black uppercase tracking-widest text-stone-500">{label}</div><textarea value={value} onChange={e=>onChange(e.target.value)} className="min-h-24 w-full rounded-2xl border border-parchment-200 bg-white p-3 text-sm font-medium outline-none focus:ring-2 focus:ring-parchment-400"/></label>}
